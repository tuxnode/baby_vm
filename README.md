# baby_vm
## Intro

Baby_VM是一个基于栈的虚拟机，具体指令的实现都依赖于虚拟栈

## 用途

由于一切指令都基于栈，包括jz跳转等指令，所以十分适合作为CTF RE的素材

可以在虚拟栈中初始化乱码包括做混淆

## 有关混淆
在将二进制指令加载到内存后,会进行解混淆

## ABI
ADD SUB等需要先将立即数压栈，在执行运算指令

## Build
### 汇编器
```
./assembler CODE.asm
```

### 虚拟机编译
```
make
```
